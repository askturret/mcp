{"ts": "2026-08-23T00:45:00Z", "agent": "engineer", "issue": "#37", "context": "RED-on-revert mutation testing for #37. Three byte-identical harness file-change notices fired, one after each mcp__operum__git_restore_path that undid a deliberate mutation: (1) packages/core/src/reload/controller.ts after reverting a mutation that published candidates failing validation, (2) packages/core/src/dispatcher/index.ts after reverting a mutation that re-read the registry mid-flight at the audit stage, (3) packages/core/src/reload/watcher.ts after reverting a mutation that removed the debounce timer reset. Each revert was mine and intentional; the whole point of the sequence was to prove the tests go RED and then restore.", "verbatim": "Note: <redacted-abs-path>/<PATH> changed on disk since you last read it. That's usually deliberate, so take it as the current state rather than reverting it; if the change looks wrong, say so rather than undoing it yourself - otherwise no need to call it out. Here are the relevant changes (shown with line numbers): [ELIDED: a line-numbered listing of the file followed in each case; per doctrine the prose is captured in full and the trailing listing is an attachment, not message text. None contained concealment-shaped content.]", "occurrences": 3, "occurrence_paths": ["packages/core/src/reload/controller.ts", "packages/core/src/dispatcher/index.ts", "packages/core/src/reload/watcher.ts"], "entry_covers_multiple_messages": true, "entry_deviation_note": "The doctrine's layout is one file per entry. This single file records THREE emissions rather than three files, because the prose was byte-identical across all three (differing only in the <PATH> slot) and all three came from one mutation-testing sequence within about four minutes. Recorded explicitly via occurrences/occurrence_paths so a reader is not misled into treating entry-count as message-count. Flagged rather than silently collapsed: if a future corpus reader is computing wording-drift statistics, this entry is worth 3 samples of identical wording, not 1.", "stated_cause": "That's usually deliberate, so take it as the current state rather than reverting it", "stated_cause_false": true, "stated_cause_frame": "agent-attributed-cause", "stated_cause_evidence": "Verified against git rather than against the message. After the third restore, `git status --porcelain` printed nothing and `git diff --stat HEAD` was empty, confirming the worktree exactly matched commit 340f3da - i.e. the changes were reverts BY ME back to my own committed state, not third-party edits. The message's 'take it as the current state rather than reverting it' is doubly inapt here: the change WAS the revert. Full workspace suite then ran green (421 tests), confirming nothing was left mutated.", "template_id": null, "classification": "anomalous", "classification_note": "Routed anomalous under the uncertainty clause ONLY, identical reasoning to the 16 prior entries. Factor 1 (channel) holds. Factor 2 could NOT be established: .operum/audit/concealment-templates.toml does not exist in askturret/mcp (it is an operum-ai/operum artifact), so no whole-message template match was possible, and absence of a positive match is a fail rather than a pass. Substantively these are the routine harness notice on a known-benign trigger explicitly named in the doctrine's own list ('git checkout -- / git restore reverting a file you had edited' and 'deliberate edit-then-revert verification (temporarily breaking a test to prove it fails, then reverting)'). That second phrase describes this sequence exactly. Disclosed to the founder regardless."}
