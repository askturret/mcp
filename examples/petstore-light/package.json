{
  "name": "petstore-light-example",
  "version": "0.1.0",
  "description": "5-minute quick start example - Petstore MCP server",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@askturret/mcp-adapters-express": "*",
    "express": "^4.18.0"
  },
  "askturret": {
    "testsNotRequired": "Runnable demo app, not a library. The facade behaviour it demonstrates (mcpFromOpenApi, Explorer route, executor wiring) is covered by packages/adapters-express tests against the same Petstore fixture."
  }
}
